const express = require('express')
const path = require('path')
module.exports = {
    database:{
        host:"localhost",
        user:"root",
        password:"",
        database:"database_links"
    }
}
